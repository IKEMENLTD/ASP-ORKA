# アフィリエイトシステムプロ - Apache設定（最小限構成）

# ======================================
# 必須設定（これがないと動かない）
# ======================================

# ディレクトリインデックス設定
DirectoryIndex index.php index.html

# ディレクトリリスティング無効化
Options -Indexes

# ======================================
# セキュリティ設定
# ======================================

# セキュリティヘッダー
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
</IfModule>

# URL Rewrite
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
</IfModule>

# .htaccessと.envの直接アクセス禁止
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# 特定ファイルタイプのアクセス制御
<FilesMatch "\.(env|log|sql|md)$">
    Order allow,deny
    Deny from all
</FilesMatch>
